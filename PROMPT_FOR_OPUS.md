# Prompt for Opus

Read the repo (especially `model/joint_alpha_model.py`, `model/RESOLUTION_alpha_lookerism.md`, `STATUS_FOR_OPUS.md`, Stream 1/2 generative modules, DECISIONS.md). Run the joint model. Do not only read the README.

## Context
TAST moved from a five-way H1–H5 product to a joint model on absorption fraction α after triage and generative Streams 1–2 showed: H2a dead; H2b ≈ H1 on growth; residual was unearned; product architecture double-counts derived streams.

Illustrative posterior: α median ~1%, upper tail low single digits — but diagnostics show **O3 (genomic) does almost all the work**; counts/growth barely move α.

## Core objection we accept
Race on the record is **lookerism**, not genomic reality.  
`implied_na = alpha` is invalid as identity.  
Modern self-ID AA autosomal NA% is at best a constraint on **Indigenous-associated ancestry retained in that cohort** (α_I), not on all administrative absorption/reclassification (α_admin).

## Ask
1. Stress-test the joint model: prior-only vs O1+O2 vs full vs O3-off; α-prior sensitivity (e.g. Beta(1,20) vs Beta(1,5) vs Beta(1,3)).
2. Propose a minimal parameterization that splits α_admin vs α_I (and f_I / dilution if needed) without reintroducing a five-way horse race.
3. What claim is actually defensible in one sentence? What must not be claimed?
4. Is the next work (a) implement the split + SE-based O3 width, (b) pull Bryc/newer cohort primary SE, or (c) lookerism observables for α_admin — and in what order?
5. Flag any remaining silent assumptions that would change the upper bound on α_I or illegitimately tighten α_admin.

Be adversarial on identification, not on the project’s right to distrust admin totals. Prefer diagnostics over new streams.
